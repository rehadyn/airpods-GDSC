header.header 
    nav.nav
        a.nav__logo(href="") Airpods 
        ul.nav__list   
            each link in links
                li.nav__item=link
                a.nav.link(href="#")=link
        button.nav__btn Beli